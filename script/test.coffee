describe "GDT", ->

  describe "app", ->

    app = new App

    it "shoudl create card and add it in todo list", ->
      expect(app).to.be.a("object")

    it "shoudl reset localStorage", ->
      expect(app).to.be.a("object")



